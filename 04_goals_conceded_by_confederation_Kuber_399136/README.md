# Do UEFA and CONMEBOL teams differ in goals conceded per match?

**Contributor:** Kuber Budhathoki — student number: s399136

Open `analyse_goals_conceded.ipynb` in Jupyter and run all cells in order. Launch Jupyter from the repository root or this topic folder. Install the root `requirements.txt` first.

- `data/data.csv`: original uploaded dataset, unchanged.
- `analyse_goals_conceded.ipynb`: original analysis, explanations and saved notebook outputs, with updated file paths.
- `outputs/`: charts exported when the notebook runs.

The notebook retains all teams in the two selected confederations. Its "stratified samples" are filtered groups, not a random sample; the author should review the sampling requirement.

The author's statistical calculations and interpretations are preserved. Original source claims have not been independently verified during organization.
